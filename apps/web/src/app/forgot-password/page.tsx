'use client'; import Link from 'next/link';import {FormEvent,useState} from 'react';import {API} from '@/lib/api';export default function Forgot(){const[msg,setMsg]=useState('');async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get('email');const r=await fetch(`${API}/auth/forgot-password`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMsg(d.token?`${d.message}. Local token: ${d.token}`:d.message)}return <main className="simple-auth"><form onSubmit={go}><h1>Reset your password</h1><p>Enter your registered email address.</p><input name="email" type="email" required placeholder="you@example.com"/><button>Generate reset link</button>{msg&&<p className="success-note">{msg}</p>}<Link href="/login">Back to sign in</Link></form></main>}
