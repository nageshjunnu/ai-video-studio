'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import {api} from '@/lib/api';export default function Analytics(){const[data,setData]=useState<any>(null);useEffect(()=>{api('/dashboard/analytics').then(setData)},[]);return <main className="module-page"><header><Link href="/">← Dashboard</Link><b>Analytics</b><span>Category view</span></header><section><p className="eyebrow">CREATION INSIGHTS</p><h1>Video analytics</h1>{!data?<div className="empty-projects">Loading analytics…</div>:<div className="analytics-grid">{[['Voices',data.voices,'voice'],['Formats',data.formats,'format'],['Languages',data.languages,'language'],['Statuses',data.statuses,'status']].map(([title,rows,key]:any)=><article key={title}><h2>{title}</h2>{rows.map((r:any)=>{const label=r[key],count=r._count;return <div className="bar-row" key={label}><span>{label}</span><i><b style={{width:`${Math.min(100,count*20)}%`}}/></i><strong>{count}</strong></div>})}</article>)}</div>}</section></main>}
