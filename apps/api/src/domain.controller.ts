import {BadRequestException,Body,Controller,Delete,Get,Param,Patch,Post,UseGuards} from '@nestjs/common';
import {CurrentUser,JwtGuard} from './auth';import {PrismaService} from './prisma.service';

@Controller('projects') @UseGuards(JwtGuard)
export class DynamicProjectsController{
 constructor(private db:PrismaService){}
 @Get() async list(@CurrentUser()u:any){await this.db.videoProject.deleteMany({where:{purgeAfter:{lte:new Date()}}});return this.db.videoProject.findMany({where:{...(u.role==='ADMIN'?{}:{userId:u.sub}),status:{not:'ARCHIVED'}},orderBy:{updatedAt:'desc'},include:{scenes:true,user:{select:{id:true,fullName:true,email:true,role:true}}}})}
 @Get('trash') trash(@CurrentUser()u:any){return this.db.videoProject.findMany({where:{...(u.role==='ADMIN'?{}:{userId:u.sub}),status:'ARCHIVED',purgeAfter:{gt:new Date()}},orderBy:{deletedAt:'desc'},include:{user:{select:{id:true,fullName:true,email:true,role:true}}}})}
 @Get(':id') one(@CurrentUser()u:any,@Param('id')id:string){return this.db.videoProject.findFirstOrThrow({where:{id,...(u.role==='ADMIN'?{}:{userId:u.sub})},include:{user:{select:{id:true,fullName:true,email:true,role:true}}}})}
 @Post() create(@CurrentUser()u:any,@Body()b:any){return this.db.videoProject.create({data:{userId:u.sub,title:b.title,script:b.script??'',voice:b.voice??'Padmavathi',language:b.language??'te',format:b.format??'16:9',template:b.template??'heritage',country:b.country??'GLOBAL',subtitleSettings:b.subtitleSettings,mediaFiles:b.mediaFiles}})}
 @Post(':id/start-render') async start(@CurrentUser()u:any,@Param('id')id:string){return this.db.$transaction(async tx=>{const project=await tx.videoProject.findFirstOrThrow({where:{id,userId:u.sub}}),estimatedMinutes=Math.max(1,Math.ceil([...project.script].length/850)),cost=estimatedMinutes*100,user=await tx.user.findUniqueOrThrow({where:{id:u.sub}});if(user.credits<cost)throw new BadRequestException(`Insufficient credits. Approximately ${cost} credits required at 100 credits/minute. Please buy credits.`);const updated=await tx.user.update({where:{id:u.sub},data:{credits:{decrement:cost}}});await tx.creditTransaction.create({data:{userId:u.sub,amount:cost,direction:'DEBIT',type:'VIDEO_RENDER',description:`Render reserve: ${project.title}`,balanceAfter:updated.credits,referenceId:id}});await tx.videoProject.update({where:{id},data:{status:'PROCESSING',progress:5,creditsConsumed:cost}});return{cost,balance:updated.credits,rate:100}})}
 @Post(':id/complete') async complete(@CurrentUser()u:any,@Param('id')id:string,@Body()b:{url:string;duration?:number;scenes?:number}){const project=await this.db.videoProject.findFirstOrThrow({where:{id,userId:u.sub}}),actualCost=Math.max(100,Math.ceil((b.duration??1)/60)*100),difference=actualCost-project.creditsConsumed,scenes=Array.from({length:Math.min(100,b.scenes??0)},(_,i)=>({projectId:id,position:i,title:`Scene ${i+1}`,content:''}));return this.db.$transaction(async tx=>{const user=await tx.user.findUniqueOrThrow({where:{id:u.sub}});if(difference>0&&user.credits<difference)throw new BadRequestException(`Render completed but ${difference} more credits are required. Please buy credits.`);let balance=user.credits;if(difference!==0){const updated=await tx.user.update({where:{id:u.sub},data:{credits:difference>0?{decrement:difference}:{increment:Math.abs(difference)}}});balance=updated.credits;await tx.creditTransaction.create({data:{userId:u.sub,amount:Math.abs(difference),direction:difference>0?'DEBIT':'CREDIT',type:difference>0?'VIDEO_RENDER':'REFUND',description:`Final duration adjustment: ${Math.ceil((b.duration??1)/60)} minute(s)`,balanceAfter:balance,referenceId:id}})}if(scenes.length){await tx.videoScene.deleteMany({where:{projectId:id}});await tx.videoScene.createMany({data:scenes})}const updated=await tx.videoProject.update({where:{id},data:{status:'COMPLETED',progress:100,finalVideoUrl:b.url,creditsConsumed:actualCost}});return{project:updated,balance,cost:actualCost}})}
 @Post(':id/fail') async fail(@CurrentUser()u:any,@Param('id')id:string){const project=await this.db.videoProject.findFirstOrThrow({where:{id,userId:u.sub}});return this.db.$transaction(async tx=>{const user=await tx.user.update({where:{id:u.sub},data:{credits:{increment:project.creditsConsumed}}});if(project.creditsConsumed)await tx.creditTransaction.create({data:{userId:u.sub,amount:project.creditsConsumed,direction:'CREDIT',type:'REFUND',description:`Failed render refund: ${project.title}`,balanceAfter:user.credits,referenceId:id}});return tx.videoProject.update({where:{id},data:{status:'FAILED',progress:0,creditsConsumed:0}})})}
 @Post(':id/restore') restore(@CurrentUser()u:any,@Param('id')id:string){return this.db.videoProject.update({where:{id,...(u.role==='ADMIN'?{}:{userId:u.sub})},data:{status:'COMPLETED',archivedAt:null,deletedAt:null,purgeAfter:null}})}
 @Patch(':id/file-url') fileUrl(@CurrentUser()u:any,@Param('id')id:string,@Body()b:{url:string}){if(!/^\/(renders|trash)\/[a-zA-Z0-9._-]+$/.test(b.url??''))throw new BadRequestException('Invalid video URL');return this.db.videoProject.update({where:{id,...(u.role==='ADMIN'?{}:{userId:u.sub})},data:{finalVideoUrl:b.url}})}
 @Patch(':id/visibility') visibility(@CurrentUser()u:any,@Param('id')id:string,@Body()b:{visibility:string}){if(!['VISIBLE','HIDDEN'].includes(b.visibility))throw new BadRequestException('Visibility must be VISIBLE or HIDDEN');return this.db.videoProject.update({where:{id,...(u.role==='ADMIN'?{}:{userId:u.sub})},data:{visibility:b.visibility}})}
 @Patch(':id') update(@CurrentUser()u:any,@Param('id')id:string,@Body()b:any){const allowed={title:b.title,country:b.country,visibility:b.visibility};return this.db.videoProject.update({where:{id,...(u.role==='ADMIN'?{}:{userId:u.sub})},data:Object.fromEntries(Object.entries(allowed).filter(([,v])=>v!==undefined))})}
 @Delete(':id') remove(@CurrentUser()u:any,@Param('id')id:string){const now=new Date();return this.db.videoProject.update({where:{id,...(u.role==='ADMIN'?{}:{userId:u.sub})},data:{status:'ARCHIVED',archivedAt:now,deletedAt:now,purgeAfter:new Date(now.getTime()+60*24*60*60*1000),visibility:'HIDDEN'}})}
}
@Controller('dashboard') @UseGuards(JwtGuard)
export class DashboardController{constructor(private db:PrismaService){}@Get()async summary(@CurrentUser()u:any){const active={userId:u.sub,status:{not:'ARCHIVED' as any}};const[user,groups,recent,transactions,paid]=await Promise.all([this.db.user.findUniqueOrThrow({where:{id:u.sub},select:{credits:true,fullName:true,role:true}}),this.db.videoProject.groupBy({by:['status'],where:active,_count:true}),this.db.videoProject.findMany({where:active,take:6,orderBy:{updatedAt:'desc'}}),this.db.creditTransaction.findMany({where:{userId:u.sub},take:10,orderBy:{createdAt:'desc'}}),this.db.purchase.count({where:{userId:u.sub,status:'SUCCEEDED'}})]);return{user:{...user,hasPaid:paid>0||user.role==='ADMIN'},statuses:Object.fromEntries(groups.map(g=>[g.status,g._count])),total:groups.reduce((n,g)=>n+g._count,0),recent,transactions}}@Get('analytics')async analytics(@CurrentUser()u:any){const[voices,formats,languages,statuses]=await Promise.all(['voice','format','language','status'].map(field=>this.db.videoProject.groupBy({by:[field as any],where:{userId:u.sub},_count:true})));return{voices,formats,languages,statuses}}}
@Controller('credits') @UseGuards(JwtGuard)
export class CreditsController{constructor(private db:PrismaService){}@Get('packages')packages(){return this.db.creditPackage.findMany({where:{active:true}})}@Get('transactions')transactions(@CurrentUser()u:any){return this.db.creditTransaction.findMany({where:{userId:u.sub},orderBy:{createdAt:'desc'}})}@Get('purchases')purchases(@CurrentUser()u:any){return this.db.purchase.findMany({where:{userId:u.sub},include:{package:true},orderBy:{createdAt:'desc'}})}@Post('purchase-request/:packageId')async request(@CurrentUser()u:any,@Param('packageId')packageId:string,@Body()b:{transactionId:string;notes?:string}){if(!b.transactionId?.trim())throw new BadRequestException('Transaction ID is required');const pack=await this.db.creditPackage.findUniqueOrThrow({where:{id:packageId}});return this.db.purchase.create({data:{userId:u.sub,packageId,credits:pack.credits,amountInPaise:pack.priceInPaise,status:'PENDING',transactionId:b.transactionId.trim(),submittedAt:new Date(),notes:b.notes}})}}
@Controller('admin') @UseGuards(JwtGuard)
export class AdminController{constructor(private db:PrismaService){}private admin(u:any){if(u.role!=='ADMIN')throw new BadRequestException('Admin access required')}@Get('dashboard')async stats(@CurrentUser()u:any){this.admin(u);const[users,videos,purchases,credits]=await Promise.all([this.db.user.count(),this.db.videoProject.groupBy({by:['status'],_count:true}),this.db.purchase.count({where:{status:'SUCCEEDED'}}),this.db.creditTransaction.aggregate({_sum:{amount:true}})]);return{users,videos:Object.fromEntries(videos.map(v=>[v.status,v._count])),purchases,creditsIssued:credits._sum.amount??0}}@Get('users')users(@CurrentUser()u:any){this.admin(u);return this.db.user.findMany({select:{id:true,fullName:true,email:true,mobile:true,role:true,credits:true,status:true,emailVerified:true,createdAt:true,lastLoginAt:true},orderBy:{createdAt:'desc'}})}@Get('videos')videos(@CurrentUser()u:any){this.admin(u);return this.db.videoProject.findMany({include:{user:{select:{fullName:true,email:true}}},orderBy:{createdAt:'desc'}})}@Get('purchases')purchases(@CurrentUser()u:any){this.admin(u);return this.db.purchase.findMany({include:{user:{select:{fullName:true,email:true}},package:true},orderBy:{createdAt:'desc'}})}@Post('purchases/:id/approve')async approve(@CurrentUser()u:any,@Param('id')id:string){this.admin(u);return this.db.$transaction(async tx=>{const p=await tx.purchase.findUniqueOrThrow({where:{id}});if(p.status!=='PENDING')throw new BadRequestException('Purchase already processed');const user=await tx.user.update({where:{id:p.userId},data:{credits:{increment:p.credits}}});const purchase=await tx.purchase.update({where:{id},data:{status:'SUCCEEDED',approvedBy:u.sub,completedAt:new Date()}});await tx.creditTransaction.create({data:{userId:p.userId,amount:p.credits,direction:'CREDIT',type:'PURCHASE',description:`Approved UPI payment ${p.transactionId}`,balanceAfter:user.credits,referenceId:id}});return purchase})}@Patch('users/:id/role')async role(@CurrentUser()u:any,@Param('id')id:string,@Body()b:{role:'ADMIN'|'CUSTOMER'|'TEST_USER'}){this.admin(u);return this.db.user.update({where:{id},data:{role:b.role}})}@Post('users/:id/credits')async adjust(@CurrentUser()u:any,@Param('id')id:string,@Body()b:{amount:number;reason?:string}){this.admin(u);if(!Number.isInteger(b.amount)||b.amount===0)throw new BadRequestException('Non-zero integer amount required');return this.db.$transaction(async tx=>{const target=await tx.user.findUniqueOrThrow({where:{id}});if(target.credits+b.amount<0)throw new BadRequestException('Insufficient user credits');const updated=await tx.user.update({where:{id},data:{credits:{increment:b.amount}}});await tx.creditTransaction.create({data:{userId:id,amount:Math.abs(b.amount),direction:b.amount>0?'CREDIT':'DEBIT',type:'ADMIN_ADJUSTMENT',description:b.reason??'Admin adjustment',balanceAfter:updated.credits}});await tx.auditLog.create({data:{actorId:u.sub,action:'CREDIT_ADJUSTMENT',entity:'User',entityId:id,metadata:{amount:b.amount}}});return{credits:updated.credits}})}}
